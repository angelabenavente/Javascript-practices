'use strict';

class square {
  perimeter(side) {
    const squarePerimeter = side * 4;
    console.log(squarePerimeter);
  }
  area(side) {
    const squareArea = side * side;
    console.log(squareArea);
  }
}

firstSquare.perimeter(9);
firstSquare.area(9);

